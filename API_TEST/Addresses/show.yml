info:
  name: Show Address
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/addresses/{{address_id}}"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{Bearer}}"

runtime:
  variables:
    - name: address_id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get a single address by ID. Requires authentication.
