info:
  name: List Articles
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}/api/articles"
  headers:
    - name: Accept
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all articles sorted by creation date. Public access.
