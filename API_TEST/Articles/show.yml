info:
  name: Show Article
  type: http
  seq: 12

http:
  method: GET
  url: "{{base_url}}/api/articles/{{article_uuid}}"
  headers:
    - name: Accept
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get a single article by UUID. Public access.
